package com.graphql.country_api.utils.constants


object DbConstants {

    /** Current version of the Room database. Increment this when schema changes. */
    const val DATABASE_VERSION: Int = 1

    /** Name of the Room database file. */
    const val DATABASE_NAME: String = "countries.db"
}
